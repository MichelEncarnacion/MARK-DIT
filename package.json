{
  "name": "mark-dit",
  "version": "1.0.0",
  "description": "MARK-DIT — bot informativo tech de la Direccion de Innovacion Tecnologica (DIT), Red SPES",
  "private": true,
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "dev": "node --watch server.js",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client install && npm --prefix client run build",
    "start": "node server.js",
    "refresh": "node scripts/refresh.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-cron": "^3.0.3",
    "rss-parser": "^3.13.0"
  }
}
